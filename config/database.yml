default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: todo-development

test:
  <<: *default
  database: todo-test

production:
  <<: *default
  database: todo-production
